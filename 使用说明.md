# 启动台 使用说明

## 1.12.1 改名与系统手势兼容

1. 应用显示名称、菜单、设置和安装包改为“启动台”；Bundle ID、内部可执行文件名与原有数据目录保持稳定。
2. 修复 macOS 26.6.2 上触控框架升级到 9460.1 后，全局五指监听因旧版本限制而停用的问题；新增支持限 Apple Silicon。
3. 未适配的系统版本会在设置中显示明确说明。接口检查与实机验收边界见 [五指手势兼容性记录](docs/multitouch-compatibility.md)。

## 1.12.0 的六项改进

1. 将应用拖到现有文件夹并短暂停留后，文件夹会在同一次拖拽中自动展开；可以直接插入某个成员之前或放到末尾，完成后继续保持文件夹打开。
2. 大文件夹支持拖拽到面板顶端或底端自动滚动；移开、取消、失焦或布局发生其他变化时会撤销临时预览，不会误保存旧位置。
3. Launch 会识别兼容的 iPhone/iPad 应用在 Apple-Silicon Mac 上的包装结构；普通应用正在更新、基础信息暂时不可读时则保留原有布局，等扫描完整后再安全同步。
4. 修复全局快捷键冲突回滚与启动偏好竞态；关闭的菜单栏图标或快捷键不会在启动瞬间短暂恢复默认。
5. 加强五指延迟显示、触控后备、拖拽隔离、设置窗口层级和退出清理，减少迟到窗口、手势失效与焦点异常。
6. 源码改用标准 MIT License，并加入贡献、安全、AI 维护与源码打包说明。

## 1.11.1 的两项改进

1. 拖拽图标期间会关闭二指分页识别；即使掌缘造成额外触点，也不会打断拖拽。松手以后要等这次触点全部离开，才重新接受下一次二指翻页。
2. 微信双开版角标保持约 20% 大小，改用暖金渐变、轻微内高光与连续双环标志，在程序坞小尺寸下更清楚、也更协调。

## 1.11.0 的五项改进

1. 页面改为严格二指横向跟手翻页；三指及更多触点整段忽略，纵向滑动与捏合不会误触分页，快速甩动仍可完成切页。
2. 五指合拢后会等触点全部抬起，再激活并稍后显示 Launch，避免 macOS App Exposé 的应用名称残留在页点上。
3. 刘海与搜索框之间增加更清楚的净距；搜索框旁的设置按钮改用同一套薄毛玻璃、着色、描边和阴影。
4. 应用右键菜单现在显示带图标的“隐藏”和“卸载”，不再使用较长的“从 Launch 中隐藏”。
5. 微信双开版的 MIUI 橙黄色双环角标稍微放大到约 16%，原微信图标和透明边界保持不变。

## 1.10.0 的四项改进

1. “微信双开版”使用 MIUI 风格的左下角橙黄色角标和两个圆环。角标会写入标准多分辨率 ICNS，主图保持原有大小与透明边界，没有灰框，也不再使用数字 `2`。
2. 主微信的右键菜单现在显示“**双开**”；确认后仍会从当前主微信执行完整的事务式重建、签名、验证与安全替换，失败时保留或恢复旧版。
3. 应用右键菜单不再提供“打开”或“编辑应用”。可卸载的普通应用会显示“卸载”，点击后仍需在共用提示中确认“移到废纸篓”；系统保护应用不会显示该项。
4. 顶部搜索与操作区域会根据当前屏幕的刘海、安全区域和辅助顶部区域自动增加间距。搜索框保持光学居中，普通屏幕的顶部布局不受影响。

## 1.9.0 的四项改进

1. “管理应用”中的搜索结果使用稳定的实时状态：隐藏或恢复时，开关、副标题和显示数量立即同步，应用随后安全迁移分组，不必关闭设置再打开。
2. 主微信与副本会显示为“微信”和“微信双开版”；文件夹缩略图的小图标矩阵也增加了四周留白，继续使用轻薄毛玻璃而没有厚灰底。
3. 这一版加入“创建或更新微信双开版…”与事务式重建；1.10.0 已将当前菜单文案精简为“**双开**”，仍会从 `/Applications/WeChat.app` 创建同卷临时副本，改为独立名称与 Bundle ID、生成角标、临时签名并校验，通过后才提交到 `/Applications/微信双开版.app`。
4. 原有副本在新副本完成验证前不会被移走；签名、验证、提交或启动失败会保留或恢复旧版。角标无法生成时会保留主包原图标，不会为视觉处理提前破坏旧副本。

## 1.8.0 的四项改进

1. 这一版首次统一“管理应用”的搜索结果、显示状态、分组和开关；1.9.0 进一步修复了原生开关复用造成的实机显示滞后。
2. 同名应用会使用应用自身的备用名称或包目录名进行区分；当时的现有副本显示为“微信生活版”。
3. 文件夹缩略图使用轻薄毛玻璃背景、微渐变和发丝描边，开启“减少透明度”时自动改用实色背景。
4. 当时可用“双开微信”直接打开已存在的微信生活版；1.9.0 已改为每次从当前主包安全重建。

## 1.7.1 的四项修复

1. 修复三指页面已经明显移动，却因抬手瞬间的触点波动偶尔回弹的问题；有效手势会使用最后一个可靠位置完成翻页。
2. 同一次三指手势只由一个触控来源完成，迟到的系统触控尾段不会重复触发；二指仍不会翻页。
3. 新配置默认使用 100 pt 图标；已有用户保存的大小不变，图标、标题、页点和程序坞之间仍会自动留出安全距离。
4. 页面内容未溢出时不再产生纵向滚动或回弹；只有空间确实不足时才启用纵向滚动兜底。

## 1.7.0 的八项改进

1. 三指滑动时优先等待底层触控通道，避免页面跟手后被 macOS 系统手势取消；放手时使用稳定的距离与同向快滑规则决定翻页或回弹。
2. 只识别水平意图明确的三指手势；上下三指和垂直为主的斜滑不会控制 Launch 页面。
3. 底部页点扩大为每页 44×44 pt 的独立点击范围。
4. “应用列表”先显示可见应用，再显示已隐藏应用；两组各自按应用的本地化名称排序。
5. 移到废纸篓前的确认提示只保留应用名、简短问句和两个必要按钮。
6. 错误警示条会在 4.6 秒后自动消失；如果期间又出现新错误，会从新计时。
7. 拖拽时顶部会出现“取消”区域，拖到其中松手会取消当次拖拽，不保存任何位置变化。
8. Launch 主窗口失去焦点或应用切到后台时会自动关闭，不会继续挡住其他软件。

## 1.6.2 的四项修复

1. 修正三指原始触点的身份解码，不再将三根手指误判为重复触点。私有触控桥没有可用帧时，本地 AppKit 三指路径会自动接管。
2. 搜索框会按当前屏幕的刘海与可见区域居中，旁边齿轮不会再将它拉向一侧。
3. 长按编辑模式只刷新正在抖动的图标叶子，拖动时停止抖动，减少整页网格的重复计算。
4. 底部错误警示条会结合当前屏幕的 Dock 位置、页点与安全间距自动上移，不再与 Dock 重叠。

## 1.6.1 的五项修复

1. 三指页面已明显移动时，松手会按实际位移方向完成翻页；抬指尾段的一指或二指不会再导致回弹。
2. 拖拽图标恢复为与鼠标 1:1 同步移动。
3. 长按编辑模式下可以直接打开文件夹，继续编辑其中的应用。
4. 将文件夹内应用拖到主页时，文件夹面板会临时淡出，便于看清外部落点；拖回或取消会恢复。
5. 放大图标时会自动压缩额外间距，但保证图标、标题、下一行和 Dock 之间不重叠。

## 1.6.0 的十二项变化

1. 拖动时不再出现白色插入线，图标会直接让位并显示落点预览。
2. 文件夹内的应用可以拖回主页面、其他页面或另一个文件夹。
3. 文件夹缩略图与展开面板去掉多余灰底和装饰，结构更简洁。
4. 拖动启动距离缩短并加入 1.2 倍温和位移增益，触控板拖动更灵敏。
5. 拖到文件夹的中心命中范围扩大，触发等待缩短到 120 毫秒。
6. 隐藏和恢复应用统一放在同一个可搜索“应用列表”中管理。
7. 主页面的设置按钮移到搜索框旁边。
8. Launch 打开时，当前屏幕顶部菜单栏和状态图标会被遮住，程序坞保持原来的显示方式。
9. 页面只响应精确三指横滑；二指不会进入任何分页路径。
10. 快速三指短滑会使用最终速度判断目标页面。
11. 五指合拢会记录逐根落指过程并容忍短暂掉点，提高打开成功率。
12. 页面圆点、图标和名称会根据当前屏幕的程序坞位置与图标大小自动留出距离。

## 安装

1. 双击 `启动台-1.12.1.dmg`。
2. 把 `启动台.app` 拖到“应用程序”文件夹。
3. 在“应用程序”中打开启动台。

启动台启动后默认显示在菜单栏，不会占用程序坞位置；菜单栏图标可在设置中关闭。

## 打开与关闭

- 默认按 `⌥ Space`：显示或隐藏启动台。可在设置中录制其他组合、用清除键停用，或恢复默认；F4 需要用户自行录制。
- 五指向内捏合：显示启动台；五指向外张开：隐藏。
- 在启动台内二指横向轻扫：页面会跟随手指连续移动，达到距离或同向快滑条件时翻页，否则回弹。三指及更多触点、纵向滑动和捏合不会进入启动台的分页路径。
- 按 `Esc`：隐藏启动台。
- 点击图标之间的空白处：隐藏启动台。
- 菜单栏图标启用时，点击它可显示、隐藏、重新扫描、打开设置或退出。
- 右键主微信并选择“**双开**”：确认后从当前主微信重新构建、校验并启动 Bundle ID 为 `com.tencent.xinWeChat2` 的副本。旧副本只会在新副本通过签名与身份校验后进入可回滚的切换步骤；失败时会显示原因并保留或恢复旧版。

## 查找与打开应用

- 在顶部搜索框输入应用名称。
- 点击图标或按 `Return` 打开当前搜索结果。
- 点击底部圆点或使用左右方向键切换页面。

## 整理应用

- 拖到另一个图标的左侧或右侧：插到对应一边；拖动过程中图标会实时让位，不显示白色插入线，取消不会保存。
- 拖到另一个应用图标的中心并短暂停留：创建文件夹。
- 拖到现有文件夹图标的中心并短暂停留：文件夹会自动展开；继续移动到成员之间或末尾，松手即可精确加入。大文件夹可在面板上下边缘自动滚动。
- 拖到网格空白处：移动到当前页面末尾。
- 拖到网格左、右边缘并短暂停留：切换到相邻页面并立即显示临时落点；继续移动可精确调整，松手只提交一次。
- 拖到顶部“取消”区域并松手：取消本次拖拽，所有应用保持原位。
- 打开文件夹后拖动其中的应用：在面板内调整顺序；拖出面板即可放回主网格、切换页面或加入另一个文件夹。
- 右键点击图标：可选择带眼睛图标的“隐藏”；普通用户应用还可选择“卸载”并进入共用的废纸篓确认。主微信另有“**双开**”，文件夹成员保留“移出文件夹”。右键菜单不再提供“打开”或“编辑应用”。
- 长按并保持图标：立即进入编辑模式；右上角的隐藏徽标适用于所有应用，左上角的删除徽标仅适用于可卸载应用。松手不会打开应用。
- 点击隐藏徽标：应用会从启动台中隐藏，后面所有页面的图标会按原有相对顺序自动向前补位。
- 点击删除徽标并确认：把应用移到废纸篓，而不是永久删除。
- 隐藏错了或需要管理多个应用：设置 →“管理应用”→“应用列表…”，搜索后直接切换每个应用的显示状态。

创建或加入文件夹会减少主网格项目数，后续页面也会自动向前补位并清除空页。文件夹会作为一个整体保留位置，成员顺序不会被跨页补位打乱。最后一页项目不足时仍从顶部排列。

## 设置

点击主界面搜索框旁的齿轮，可调整：

- 每页行数与列数
- 图标大小
- 是否显示应用名称
- 是否显示菜单栏图标
- 全局快捷键：录制新组合、清除以停用，或恢复 `⌥ Space`
- 是否登录时自动启动
- 在一个应用列表中管理显示与隐藏状态
- 重新扫描应用程序

布局和设置会自动保存到 `~/Library/Application Support/Launch`。

启动台会监听系统和用户的“应用程序”目录。新应用安装完成后通常会在一秒内自动出现在网格末尾，无需退出重开。应用名称会优先采用应用自带的中文本地化名称。

## 注意事项

- 启动台当前使用二指横向分页，三指及更多触点继续交给 macOS，因此不会尝试拦截 Mission Control、App Exposé 或 Spaces。五指合拢被识别后会请求显示窗口；启动台不会重启程序坞或永久修改系统手势。
- 全局五指识别仅在已经适配的 macOS 触控层版本上启用；系统更新后若暂时不可用，可使用已配置的全局快捷键、菜单栏图标，或直接从“应用程序”文件夹打开启动台。
- 如果同时关闭菜单栏图标和全局快捷键，仍可从“应用程序”文件夹打开启动台并重新进入设置。
- “卸载”只会把所选 `.app` 移到废纸篓；系统保护的应用不会显示删除按钮。
- `Releases/Launch.dmg` 是 Apple Silicon（arm64）个人版本，`Releases/Launch-1.12.0-x86_64.dmg` 是 Intel 个人版本；两者都使用本机临时签名。如需发给其他人，应换用 Apple Developer ID 签名并完成 Apple 公证。
